{
  "name": "olatu2",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently -k -n frontend,backend -c blue,green \"npm --prefix frontend run dev\" \"npm --prefix backend run dev\"",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:backend": "npm --prefix backend run dev",
    "build": "npm --prefix frontend run build && npm --prefix backend run build"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
